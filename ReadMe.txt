
1. create github account and remote repository 

2. create local git repository (git init)

3. create "Hello World" Console Project and add it into local repository (git add ./) (git commit -m "msg")

4. local repository add remote repository (git remote add origin <url>)

5. git push orgin master

6. create a new branch (git branch dev01)

